#pragma once

#include <string>

namespace FileManCore {
    namespace Utils {
        namespace Win {
            namespace NapiHelpers {
                std::u16string GetLastErrorAsString();
            }
        }
    }
}