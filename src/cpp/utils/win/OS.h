#pragma once

#include <string>

namespace FileManCore {
    namespace Utils {
        namespace Win {
            namespace OS {
                std::u16string GetLastErrorAsString();
            }
        }
    }
}